import { HeroParallaxDemo } from "@/components/wrappers/hero";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between overflow-hidden p-16 py-0">
      <HeroParallaxDemo />
    </main>
  );
}
